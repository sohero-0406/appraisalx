<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.common.dao.ExamUserDao">
	
	<!-- 查询数据
	<select id="findList" resultType="ExamUser">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<!-- 查询正在考试中的考试 -->
	<select id="getExamingUser" resultType="ExamUser">
		SELECT * FROM common_exam_user
		WHERE id = #{id} AND exam_id = #{examId} AND state = 3
	</select>

</mapper>